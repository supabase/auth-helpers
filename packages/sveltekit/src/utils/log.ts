import * as loglevel from 'loglevel';

const logger = loglevel.getLogger('@supabase/auth-helpers-sveltekit');
logger.info('Starting @supabase/auth-helpers-sveltekit logging...');

export default logger;
